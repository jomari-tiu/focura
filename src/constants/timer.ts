export const SESSION_25_SECONDS = 25 * 60; // 1500
export const SESSION_50_SECONDS = 50 * 60; // 3000
export const SHORT_BREAK_SECONDS = 5 * 60; // 300
export const LONG_BREAK_SECONDS = 15 * 60; // 900

export const RING_RADIUS = 120;
export const STROKE_WIDTH = 12;
export const CIRCUMFERENCE = 2 * Math.PI * RING_RADIUS; // ~753.98

// SVG viewBox center
export const SVG_SIZE = (RING_RADIUS + STROKE_WIDTH) * 2; // 264
export const SVG_CENTER = SVG_SIZE / 2; // 132
